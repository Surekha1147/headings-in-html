# Day 2 – HTML Heading Tags

## 📌 Overview
This project demonstrates the use of HTML heading tags from `<h1>` to `<h6>`.

## 🎯 Objective
To understand the hierarchy and structure of heading elements in HTML.

## 🏷 Tags Used
- `<h1>` – Main heading (largest and most important)
- `<h2>` – Subheading
- `<h3>` – Section heading
- `<h4>` – Subsection heading
- `<h5>` – Minor heading
- `<h6>` – Smallest heading

## 🧠 What I Learned
- Heading tags define the structure of a webpage.
- `<h1>` is the most important heading.
- Proper hierarchy improves readability and SEO.
- Headings should be used in order (h1 → h6).

## 🚀 Skills Practiced
- Basic HTML structure
- Proper indentation
- Understanding semantic meaning of headings
